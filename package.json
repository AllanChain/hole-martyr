{
  "name": "hole-martyr",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -n Vite,FastAPI -c magenta,green \"vite\" \"poetry run uvicorn api:app --reload\"",
    "build": "vue-tsc --noEmit && vite build",
    "serve": "poetry run uvicorn api:app"
  },
  "dependencies": {
    "light-date": "^1.2.0",
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^0.23.1",
    "@vitejs/plugin-vue": "^2.3.3",
    "concurrently": "^7.2.0",
    "eslint": "^8.15.0",
    "typescript": "^4.5.4",
    "vite": "^2.9.9",
    "vue-tsc": "^0.34.7"
  }
}
